# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working
with code in this repository.

`AGENTS.md` is the canonical rulebook for agents in this repository: the
stack and commands, the quality gates, the testing policy (no mocks for
the PTY, the emulator, or the protocol), the `!Send` constraint, and the
git worktree layout. Read it before doing any work. This file adds
nothing beyond it.
